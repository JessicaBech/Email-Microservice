# Spring Properties
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
spring:
  mail:
    host: "smtp.gmail.com"
    port: 587
    username: "testingaccoun357@gmail.com"
    password: "cqsjwvrmdvzaizyx"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /tr-email-microservice/

# Management Configuration
management:
  endpoint:
    web:
      exposure:
        include: "*"
    health:
      show-details: always